{
  "name": "push-vibes",
  "displayName": "Push Vibes",
  "description": "Push your vibes to git",
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.74.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [],
  "main": "./out/extension.js",
  "packageManager": "pnpm@10.12.4",
  "contributes": {
    "commands": [
      {
        "command": "push-vibes.pushVibes",
        "title": "Push Vibes ✨"
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "pnpm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./"
  },
  "devDependencies": {
    "@types/node": "16.x",
    "@types/vscode": "^1.74.0",
    "@vscode/vsce": "^3.6.0",
    "typescript": "^4.9.4"
  }
}
